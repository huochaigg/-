# V10 评分记录（写完空白卷再填）

满分 100。每题旁标 A / B / C：独立写出 / 思路有但卡住 / 完全下不了手。

| 题 | 分 | 状态 A/B/C | 卡在哪 |
|---|---|---|---|
| debounce | 10 | | |
| Promise.all | 15 | | |
| 并发控制 | 20 | | |
| EventEmitter | 15 | | |
| deepClone | 10 | | |
| bind | 10 | | |
| new + instanceof | 10 | | |
| flat | 5 | | |
| Node / Event Loop | 5 | | |
| **合计** | **100** | | |

场景题（生图执行器）不单独计分，能讲清设计即可：单任务 `retry(timeout(generate))`，外层并发 3，结果按 index 收。

## 怎么用分

- **≥ 80**：第一阶段过，可以 V11 数组转树
- **60～79**：也进 V11，不会的题每天手写一道
- **40～59**：不全部重学，只补 Promise.all、并发控制、EventEmitter、bind
- **&lt; 40**：前面主要是看懂，再把这四道核心重写两遍

## 四道最值钱的

1. 并发控制  
2. Promise.all  
3. EventEmitter  
4. bind  

## Event Loop 自核（写完再看）

浏览器大致：`1` → `3` `4` → `2`（同步 → 微任务 then/queueMicrotask → timer）

Node 再多：`process.nextTick` 一般先于 Promise 微任务；`setImmediate` 在 check 阶段。V10 不钻 phase。
